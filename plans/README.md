# Implementation Plans

Generated by the improve skill on 2026-06-18. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add ruff (linter, formatter, import sorter) | P1 | S | — | TODO |
| 002  | Add GitHub Actions CI | P1 | S | 001 | TODO |
| 003  | Extract outline table parser from parser.py | P2 | M | — | TODO |
| 004  | Clean up dead code and add missing tests | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with reason) | REJECTED (with rationale)

## Dependency notes

- 002 requires 001 because CI must be able to run ruff checks.

## Findings considered and rejected

- **parser.py column_index linear scan**: O(n) scan per transform is fine at CLI data sizes (~columns ≤ 50). Optimizing to a dict lookup adds complexity for zero measurable benefit.
- **No type checker (mypy/pyright)**: ruff catches a overlapping set of issues at much lower configure/run cost. Adding a full type checker to a 1800-line CLI is disproportionate — if the project grows, revisit.
- **`--stats` percentage column detection**: a direction-level suggestion, not a code quality finding. Worth a feature request, not a plan.
- **`-o / --output` file flag**: similar — feature request, not a finding.
